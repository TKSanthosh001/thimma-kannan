[build]
  command = "npm run build"
  publish = "dist"

# Ignore builds on branches other than main to conserve Netlify credits
[build.processing]
  skip_processing = false

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
